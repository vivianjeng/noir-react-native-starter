[package]
name = "srs_downloader"
version = "0.1.0"
edition = "2021"

[dependencies]
noir = { git = "https://github.com/zkmopro/noir-rs.git", features = ["barretenberg"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
